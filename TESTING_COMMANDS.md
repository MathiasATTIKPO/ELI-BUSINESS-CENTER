# 🧪 TEST AUTOMATION & VERIFICATION COMMANDS\n\n## Quick Verification Script\n\nVérifiez rapidement que toutes les corrections fonctionnent :\n\n### 1. Test Backend API Endpoints\n\n```bash\n# Obtenir un token admin\nTOKEN=$(curl -s -X POST http://localhost:4001/api/admin/login \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"email\":\"admin\",\"password\":\"password123\"}' \\\n  | jq -r '.data.token')\n\necho \"Token obtenu: $TOKEN\"\n\n# Test 1: GET /api/admin/sales (BUG #1 - CORRECTED)\necho \"\"\necho \"=== TEST 1: GET /api/admin/sales ===\"\ncurl -s -X GET http://localhost:4001/api/admin/sales \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" | jq '.\n\n# Test 2: GET /api/admin/stats (BUG #1 - CORRECTED)\necho \"\"\necho \"=== TEST 2: GET /api/admin/stats ===\"\ncurl -s -X GET http://localhost:4001/api/admin/stats \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" | jq '.\n\n# Test 3: GET /api/admin/repairs\necho \"\"\necho \"=== TEST 3: GET /api/admin/repairs ===\"\ncurl -s -X GET http://localhost:4001/api/admin/repairs \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" | jq '.\n\n# Test 4: GET /api/admin/notifications\necho \"\"\necho \"=== TEST 4: GET /api/admin/notifications ===\"\ncurl -s -X GET http://localhost:4001/api/admin/notifications \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" | jq '.\n```\n\n### 2. Postman Collection Tests\n\nImport cette collection dans Postman et testez chaque endpoint :\n\n```json\n{\n  \"info\": {\n    \"name\": \"ELI Business Center - Corrections Test\",\n    \"schema\": \"https://schema.getpostman.com/json/collection/v2.1.0/collection.json\"\n  },\n  \"item\": [\n    {\n      \"name\": \"Auth\",\n      \"item\": [\n        {\n          \"name\": \"Login Admin\",\n          \"event\": [\n            {\n              \"listen\": \"test\",\n              \"script\": {\n                \"exec\": [\n                  \"if (pm.response.code === 200) {\",\n                  \"    var jsonData = pm.response.json();\",\n                  \"    pm.environment.set(\\\"token\\\", jsonData.data.token);\",\n                  \"    pm.test(\\\"Token stored\\\", function () {\",\n                  \"        pm.expect(jsonData.data.token).to.be.ok;\",\n                  \"    });\",\n                  \"}\"\n                ]\n              }\n            }\n          ],\n          \"request\": {\n            \"method\": \"POST\",\n            \"header\": [{\"key\": \"Content-Type\", \"value\": \"application/json\"}],\n            \"body\": {\n              \"mode\": \"raw\",\n              \"raw\": \"{\\\"email\\\":\\\"admin\\\",\\\"password\\\":\\\"password123\\\"}\"\n            },\n            \"url\": {\"raw\": \"http://localhost:4001/api/admin/login\", \"protocol\": \"http\", \"host\": [\"localhost\"], \"port\": \"4001\", \"path\": [\"api\", \"admin\", \"login\"]}\n          }\n        }\n      ]\n    },\n    {\n      \"name\": \"Sales - CORRECTED ✅\",\n      \"item\": [\n        {\n          \"name\": \"GET /api/admin/sales\",\n          \"event\": [\n            {\n              \"listen\": \"test\",\n              \"script\": {\n                \"exec\": [\n                  \"pm.test(\\\"Status code 200\\\", function () { pm.response.to.have.status(200); });\",\n                  \"pm.test(\\\"Response has success: true\\\", function () { pm.expect(pm.response.json().success).to.equal(true); });\",\n                  \"pm.test(\\\"Response has data array\\\", function () { pm.expect(pm.response.json().data).to.be.an('array'); });\"\n                ]\n              }\n            }\n          ],\n          \"request\": {\n            \"method\": \"GET\",\n            \"header\": [{\"key\": \"Authorization\", \"value\": \"Bearer {{token}}\"}],\n            \"url\": {\"raw\": \"http://localhost:4001/api/admin/sales\", \"protocol\": \"http\", \"host\": [\"localhost\"], \"port\": \"4001\", \"path\": [\"api\", \"admin\", \"sales\"]}\n          }\n        }\n      ]\n    },\n    {\n      \"name\": \"Stats - CORRECTED ✅\",\n      \"item\": [\n        {\n          \"name\": \"GET /api/admin/stats\",\n          \"event\": [\n            {\n              \"listen\": \"test\",\n              \"script\": {\n                \"exec\": [\n                  \"pm.test(\\\"Status code 200\\\", function () { pm.response.to.have.status(200); });\",\n                  \"pm.test(\\\"Response has success: true\\\", function () { pm.expect(pm.response.json().success).to.equal(true); });\",\n                  \"pm.test(\\\"Response has totalRepairs\\\", function () { pm.expect(pm.response.json().data.totalRepairs).to.be.ok; });\",\n                  \"pm.test(\\\"Response has totalRevenue\\\", function () { pm.expect(pm.response.json().data.totalRevenue).to.be.a('number'); });\"\n                ]\n              }\n            }\n          ],\n          \"request\": {\n            \"method\": \"GET\",\n            \"header\": [{\"key\": \"Authorization\", \"value\": \"Bearer {{token}}\"}],\n            \"url\": {\"raw\": \"http://localhost:4001/api/admin/stats\", \"protocol\": \"http\", \"host\": [\"localhost\"], \"port\": \"4001\", \"path\": [\"api\", \"admin\", \"stats\"]}\n          }\n        }\n      ]\n    },\n    {\n      \"name\": \"Notifications - CORRECTED ✅\",\n      \"item\": [\n        {\n          \"name\": \"GET /api/admin/notifications\",\n          \"event\": [\n            {\n              \"listen\": \"test\",\n              \"script\": {\n                \"exec\": [\n                  \"pm.test(\\\"Status code 200\\\", function () { pm.response.to.have.status(200); });\",\n                  \"pm.test(\\\"Response has success: true\\\", function () { pm.expect(pm.response.json().success).to.equal(true); });\",\n                  \"pm.test(\\\"Response has data array\\\", function () { pm.expect(pm.response.json().data).to.be.an('array'); });\"\n                ]\n              }\n            }\n          ],\n          \"request\": {\n            \"method\": \"GET\",\n            \"header\": [{\"key\": \"Authorization\", \"value\": \"Bearer {{token}}\"}],\n            \"url\": {\"raw\": \"http://localhost:4001/api/admin/notifications\", \"protocol\": \"http\", \"host\": [\"localhost\"], \"port\": \"4001\", \"path\": [\"api\", \"admin\", \"notifications\"]}\n          }\n        }\n      ]\n    }\n  ]\n}\n```\n\n---\n\n## Frontend Console Tests\n\nOuvrez DevTools (F12) et collez ces commandes dans la console :\n\n### Test 1️⃣ : Vérifier que Dashboard.jsx charge sans erreur\n\n```javascript\n// Dans la console du Dashboard admin\n\n// Vérifier qu'il n'y a pas d'erreur\nif (window.lastError) {\n  console.error('❌ Error found:', window.lastError);\n} else {\n  console.log('✅ No errors in console');\n}\n\n// Vérifier que les data sont chargées\nconst dashboardData = document.querySelector('[data-testid=\"sales-tab\"]');\nif (dashboardData) {\n  console.log('✅ Sales tab found');\n} else {\n  console.warn('⚠️ Sales tab not found');\n}\n```\n\n### Test 2️⃣ : Vérifier les requêtes API\n\n```javascript\n// Voir toutes les requêtes API et leurs statuts\n// DevTools → Network → Filter: Fetch/XHR\n\n// Vérifier que /api/admin/sales retourne 200\nfetch('http://localhost:4001/api/admin/sales', {\n  headers: {\n    'Authorization': `Bearer ${localStorage.getItem('adminToken')}`\n  }\n})\n.then(res => {\n  console.log('Sales endpoint status:', res.status);\n  return res.json();\n})\n.then(data => {\n  console.log('Sales data:', data);\n  if (data.success && data.data.length > 0) {\n    console.log('✅ Sales endpoint working correctly');\n  } else {\n    console.warn('⚠️ Sales endpoint returned empty data');\n  }\n})\n.catch(err => console.error('❌ Error:', err));\n```\n\n### Test 3️⃣ : Vérifier que variables ne sont pas undefined\n\n```javascript\n// Vérifier que salesRes n'est plus undefined\n// (Cela devrait s'afficher dans React DevTools)\n\nif (typeof window.__REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined') {\n  console.log('✅ React DevTools available');\n  // Inspecter les props du composant Dashboard\n} else {\n  console.warn('Install React DevTools to inspect component props');\n}\n```\n\n---\n\n## Automated Testing with Jest (Optional)\n\nSi vous voulez des tests unitaires :\n\n### Install Jest\n```bash\ncd backend\nnpm install --save-dev jest supertest\n```\n\n### Create test file: `backend/__tests__/admin.test.js`\n\n```javascript\nconst request = require('supertest');\nconst app = require('../server');\n\ndescribe('Admin API - CORRECTIONS TEST', () => {\n  let token;\n\n  beforeAll(async () => {\n    // Login to get token\n    const res = await request(app)\n      .post('/api/admin/login')\n      .send({ email: 'admin', password: 'password123' });\n    token = res.body.data.token;\n  });\n\n  describe('GET /api/admin/sales - BUG #1 CORRECTED', () => {\n    it('should return 200 status', async () => {\n      const res = await request(app)\n        .get('/api/admin/sales')\n        .set('Authorization', `Bearer ${token}`);\n      expect(res.status).toBe(200);\n    });\n\n    it('should return success: true', async () => {\n      const res = await request(app)\n        .get('/api/admin/sales')\n        .set('Authorization', `Bearer ${token}`);\n      expect(res.body.success).toBe(true);\n    });\n\n    it('should return data array', async () => {\n      const res = await request(app)\n        .get('/api/admin/sales')\n        .set('Authorization', `Bearer ${token}`);\n      expect(Array.isArray(res.body.data)).toBe(true);\n    });\n\n    it('should NOT throw ReferenceError', async () => {\n      const res = await request(app)\n        .get('/api/admin/sales')\n        .set('Authorization', `Bearer ${token}`);\n      expect(res.status).not.toBe(500);\n    });\n  });\n\n  describe('GET /api/admin/stats - BUG #1 CORRECTED', () => {\n    it('should return 200 status', async () => {\n      const res = await request(app)\n        .get('/api/admin/stats')\n        .set('Authorization', `Bearer ${token}`);\n      expect(res.status).toBe(200);\n    });\n\n    it('should have totalRepairs', async () => {\n      const res = await request(app)\n        .get('/api/admin/stats')\n        .set('Authorization', `Bearer ${token}`);\n      expect(res.body.data.totalRepairs).toBeGreaterThanOrEqual(0);\n    });\n\n    it('should have totalRevenue', async () => {\n      const res = await request(app)\n        .get('/api/admin/stats')\n        .set('Authorization', `Bearer ${token}`);\n      expect(typeof res.body.data.totalRevenue).toBe('number');\n    });\n\n    it('should NOT use Inventory model (should use InventoryItem)', async () => {\n      const res = await request(app)\n        .get('/api/admin/stats')\n        .set('Authorization', `Bearer ${token}`);\n      expect(res.status).not.toBe(500);\n      expect(res.body.message).not.toContain('Cannot find module');\n    });\n  });\n\n  describe('Notification Triggers - BUG #3 CORRECTED', () => {\n    it('should create notification when tradein accepted', async () => {\n      // This test depends on actual data\n      // Simplified for demonstration\n      const res = await request(app)\n        .get('/api/admin/notifications')\n        .set('Authorization', `Bearer ${token}`);\n      expect(res.status).toBe(200);\n    });\n  });\n});\n```\n\n### Run tests\n```bash\nnpm test\n```\n\n---\n\n## Performance Monitoring\n\n### Check Response Times\n\n```bash\n# Backend response time for /api/admin/sales\ntime curl -s http://localhost:4001/api/admin/sales -H \"Authorization: Bearer $TOKEN\" > /dev/null\n\n# Should be < 500ms for good performance\n```\n\n### Monitor MongoDB Performance\n\n```bash\n# Connect to MongoDB\nmongo\n\n# Enable profiling\nuse eli-business-center\ndb.setProfilingLevel(2)\n\n# See slow queries\ndb.system.profile.find({ millis: { $gt: 100 } }).pretty()\n```\n\n---\n\n## Error Scenario Testing\n\n### Test 1: Missing Token\n```bash\ncurl -s http://localhost:4001/api/admin/sales\n# Expected: 401 Unauthorized\n```\n\n### Test 2: Invalid Token\n```bash\ncurl -s -H \"Authorization: Bearer invalid_token\" http://localhost:4001/api/admin/sales\n# Expected: 401 Unauthorized\n```\n\n### Test 3: Non-existent Resource\n```bash\ncurl -s -H \"Authorization: Bearer $TOKEN\" http://localhost:4001/api/admin/sales/999\n# Expected: 404 Not Found\n```\n\n---\n\n## Logging & Debugging\n\n### Enable Debug Mode\n\n```bash\n# Backend with debug logging\nDEBUG=eli-* npm run dev\n\n# Or add to .env\nDEBUG_MODE=true\nLOG_LEVEL=debug\n```\n\n### Check Logs\n\n```bash\n# See all backend logs\ntail -f backend.log\n\n# Filter by error\ngrep ERROR backend.log\n\n# Filter by API endpoint\ngrep /api/admin/sales backend.log\n```\n\n---\n\n## Final Verification Checklist\n\nRun this checklist to confirm all fixes are working :\n\n```bash\n# 1. Backend running\ncurl -s http://localhost:4001 && echo \"✅ Backend OK\" || echo \"❌ Backend DOWN\"\n\n# 2. Get admin token\nTOKEN=$(curl -s -X POST http://localhost:4001/api/admin/login \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"email\":\"admin\",\"password\":\"password123\"}' \\\n  | jq -r '.data.token')\n[ -n \"$TOKEN\" ] && echo \"✅ Token obtained\" || echo \"❌ Failed to get token\"\n\n# 3. Sales endpoint working\ncurl -s -X GET http://localhost:4001/api/admin/sales \\\n  -H \"Authorization: Bearer $TOKEN\" | jq '.success' | grep -q 'true' && echo \"✅ /api/admin/sales OK\" || echo \"❌ /api/admin/sales FAILED\"\n\n# 4. Stats endpoint working  \ncurl -s -X GET http://localhost:4001/api/admin/stats \\\n  -H \"Authorization: Bearer $TOKEN\" | jq '.success' | grep -q 'true' && echo \"✅ /api/admin/stats OK\" || echo \"❌ /api/admin/stats FAILED\"\n\n# 5. Frontend dashboard accessible\ncurl -s http://localhost:3000/admin | grep -q 'html' && echo \"✅ Frontend OK\" || echo \"❌ Frontend DOWN\"\n\necho \"\"\necho \"All critical tests passed! ✅✅✅\"\n```\n\n---\n\n**Run these tests after deploying to verify all corrections work correctly!** 🚀\n